import styled from "styled-components";

export const InputContainer = styled.div`
    width: 90%;
    height: 4rem;
    background: #000;
    border-radius: 50rem;

    margin: 2rem;
    padding: 0;
    
    input {
        background: transparent;
        border: 0;
        border-radius: 50rem;
        width: 95%;
        height: 4rem;
        padding: 0 2rem;
        margin: 0;
        color: var(--white);
        font-size: 1rem;
    }
`